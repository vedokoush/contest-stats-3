# Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PREHSG Contest Hub                               │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐    ┌──────────────────────────────┐
│        FRONTEND (React+Vite)      │    │    BACKEND (FastAPI)         │
│                                  │    │                              │
│  http://localhost:5173          │    │  http://localhost:8000       │
│                                  │    │                              │
│  ┌────────────────────────────┐  │    │  ┌────────────────────────┐  │
│  │  App.tsx (Router)          │  │    │  │  main.py (FastAPI)     │  │
│  └────────────┬───────────────┘  │    │  │  - CORS enabled        │  │
│               │                   │    │  │  - Routes included     │  │
│  ┌────────────▼───────────────┐  │    │  └────────────┬───────────┘  │
│  │  HomePage.tsx              │  │    │               │              │
│  │  ├─ Header.tsx             │  │    │  ┌────────────▼───────────┐  │
│  │  │  (Class selection)      │  │    │  │  routers/contests.py  │  │
│  │  │                         │  │    │  │  ├─ GET /contests     │  │
│  │  └─ Sidebar.tsx            │  │    │  │  ├─ POST /contests    │  │
│  │     (Year accordion)       │  │    │  │  ├─ PUT /contests/{id}│  │
│  │     └─ ContestCard.tsx     │  │    │  │  └─ DELETE /contests/ │  │
│  │        (Contest display)   │  │    │  └────────────┬───────────┘  │
│  │                            │  │    │               │              │
│  │  AdminPage.tsx             │  │    │  ┌────────────▼───────────┐  │
│  │  └─ AdminPanel.tsx         │  │    │  │  models.py             │  │
│  │     (CRUD interface)       │  │    │  │  ├─ Contest (SQLModel) │  │
│  │     ├─ Add Dialog          │  │    │  │  ├─ ContestCreate     │  │
│  │     ├─ Edit Form           │  │    │  │  ├─ ContestUpdate     │  │
│  │     ├─ Delete Confirmation │  │    │  │  └─ ContestRead       │  │
│  │     └─ Table View          │  │    │  └────────────┬───────────┘  │
│  │                            │  │    │               │              │
│  │  Components:               │  │    │  ┌────────────▼───────────┐  │
│  │  ├─ Button.tsx             │  │    │  │  database.py           │  │
│  │  ├─ Header.tsx             │  │    │  │  ├─ Engine (SQLite)   │  │
│  │  ├─ Sidebar.tsx            │  │    │  │  └─ Session           │  │
│  │  ├─ ContestCard.tsx        │  │    │  └────────────────────────┘  │
│  │  └─ AdminPanel.tsx         │  │    │                              │
│  │                            │  │    │  ┌────────────────────────┐  │
│  │  Services:                 │  │    │  │  init_db.py            │  │
│  │  └─ lib/api.ts             │  │    │  │  (Sample data init)    │  │
│  │     (Axios API client)     │  │    │  └────────────────────────┘  │
│  └────────────────────────────┘  │    └──────────────────────────────┘
│                                  │
│  Styling:                        │    Database:
│  - Tailwind CSS                  │    - SQLite (contest_hub.db)
│  - shadcn/ui components          │    - SQLModel ORM
│  - Framer Motion animations      │    - Type-safe queries
│  - Responsive design             │    - Automatic table creation
│                                  │
│  Build Tool:                     │
│  - Vite (lightning-fast)         │    CORS:
│                                  │    - localhost:5173 ✓
└──────────────────────────────────┘    - localhost:3000 ✓
                                       └──────────────────────────────┘

                        ▼ AXIOS HTTP CLIENT ▼
                    
                /api/contests (proxy)
                        │
                        ▼
                   localhost:8000
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│              USER INTERACTION                            │
│           (Browser at localhost:5173)                   │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│           REACT COMPONENT RENDERS                        │
│  (Header → Sidebar → ContestCard OR AdminPanel)         │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│        USER CLICKS BUTTON / FORMS SUBMIT                │
│        (Create, Edit, Delete, View Links)              │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│         API CLIENT (lib/api.ts)                          │
│         - Axios HTTP request                            │
│         - POST, GET, PUT, DELETE                        │
│         - Error handling                                │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│    VITE PROXY: /api → localhost:8000                    │
│         (Development only)                              │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│       FASTAPI ROUTER (routers/contests.py)              │
│       - Route matching                                  │
│       - Request validation (Pydantic)                   │
│       - Business logic                                  │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│        DATABASE (SQLite via SQLModel)                   │
│        ├─ SELECT (Read)                                 │
│        ├─ INSERT (Create)                               │
│        ├─ UPDATE (Modify)                               │
│        └─ DELETE (Remove)                               │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│       RESPONSE (JSON)                                    │
│       ├─ Status code (200, 201, 204, 404)              │
│       ├─ Data (Contest object or array)                │
│       └─ Error message (if applicable)                 │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│    FRONTEND RECEIVES & PROCESSES                        │
│    ├─ Parse JSON response                               │
│    ├─ Update component state                            │
│    └─ Show success/error message                        │
└─────────────┬───────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────┐
│    REACT RE-RENDERS                                     │
│    ├─ Animations trigger                                │
│    ├─ UI updates                                        │
│    └─ User sees changes                                 │
└─────────────────────────────────────────────────────────┘
```

---

## Component Hierarchy

```
App (Router)
│
├── HomePage
│   ├── Header (Navigation + Class Selection)
│   │   ├── Logo
│   │   ├── Class Buttons (9, 10, 11, 12)
│   │   └── Admin Link Button
│   │
│   └── Sidebar (Year Accordion)
│       ├── Accordion.Root (from Radix UI)
│       │   ├── Accordion.Item (Year 2025)
│       │   │   ├── ContestCard (Pre #1)
│       │   │   ├── ContestCard (Pre #2)
│       │   │   └── ContestCard (Pre #3)
│       │   │
│       │   └── Accordion.Item (Year 2024)
│       │       └── ...ContestCards
│       │
│       └── ContestCard (Reusable)
│           ├── Title
│           ├── Pre Badge
│           ├── Button (Contest Link)
│           └── Button (Solution Link)
│
└── AdminPage
    ├── Header (with Back Button)
    │
    └── AdminPanel (CRUD Interface)
        ├── Dialog (Add Contest)
        │   ├── Form Fields
        │   └── Submit Button
        │
        └── Table View
            ├── Table Headers
            └── Table Rows
                ├── Edit Button (opens Dialog)
                └── Delete Button (with confirmation)
```

---

## API Endpoint Structure

```
BASE_URL: http://localhost:8000

├── [Health & Info]
│   ├── GET /health
│   └── GET /
│
├── [Main CRUD]
│   ├── GET /contests              → All contests
│   ├── POST /contests             → Create new
│   ├── GET /contests/{id}         → Get one
│   ├── PUT /contests/{id}         → Update
│   └── DELETE /contests/{id}      → Delete
│
├── [Filters]
│   ├── GET /contests/class/{9-12} → By class
│   └── GET /contests/year/{YYYY}  → By year
│
└── [Documentation]
    ├── GET /docs                  → Swagger UI
    └── GET /redoc                 → ReDoc
```

---

## Database Schema

```
contests table
│
├─ id (INTEGER, PRIMARY KEY)
│  └─ Auto-incremented unique identifier
│
├─ class_level (INTEGER, NOT NULL)
│  └─ Value: 9, 10, 11, or 12
│
├─ year (INTEGER, NOT NULL)
│  └─ Value: 2000-2100
│
├─ pre_number (INTEGER, NOT NULL)
│  └─ Value: 1, 2, or 3
│
├─ contest_url (STRING, NOT NULL)
│  └─ External contest link
│
└─ solution_url (STRING, NOT NULL)
   └─ External solution link
```

---

## Technology Stack Overview

```
┌──────────────────────────────────────────────────────────────┐
│                   PREHSG Contest Hub                          │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  Frontend Layer:                                              │
│  ├─ React 18             (UI Framework)                       │
│  ├─ Vite 5               (Build tool)                         │
│  ├─ TypeScript            (Type Safety)                       │
│  ├─ Tailwind CSS          (Styling)                           │
│  ├─ shadcn/ui            (Components)                         │
│  ├─ Framer Motion        (Animations)                         │
│  ├─ React Router         (Navigation)                         │
│  ├─ Axios                (HTTP Client)                        │
│  └─ Radix UI             (Primitives)                         │
│                                                               │
│  Backend Layer:                                               │
│  ├─ FastAPI              (Web Framework)                      │
│  ├─ SQLModel             (ORM + Validation)                   │
│  ├─ SQLAlchemy           (SQL Toolkit)                        │
│  ├─ Pydantic             (Data Validation)                    │
│  ├─ Uvicorn              (ASGI Server)                        │
│  └─ Python 3.9+          (Language)                           │
│                                                               │
│  Database Layer:                                              │
│  ├─ SQLite               (Default DB)                         │
│  └─ PostgreSQL Ready     (Production Ready)                   │
│                                                               │
│  DevOps Layer:                                                │
│  ├─ npm                  (Frontend Package Manager)           │
│  ├─ pip                  (Python Package Manager)             │
│  ├─ Git                  (Version Control)                    │
│  ├─ Docker Ready         (Containerization)                   │
│  └─ Environment Scripts  (Setup Automation)                   │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## Development Environment

```
┌────────────────────────────────────────────────────┐
│         Development Setup                          │
│                                                    │
│  Frontend Dev Server:                              │
│  $ npm run dev                                     │
│  Vite @ localhost:5173                             │
│  ├─ Hot Module Replacement (HMR)                   │
│  ├─ Fast refresh on file change                    │
│  └─ API proxy to localhost:8000                    │
│                                                    │
│  Backend Dev Server:                               │
│  $ python -m uvicorn main:app --reload             │
│  Uvicorn @ localhost:8000                          │
│  ├─ Auto-reload on file change                     │
│  ├─ Interactive docs @ /docs                       │
│  └─ ReDoc docs @ /redoc                            │
│                                                    │
│  Database:                                         │
│  SQLite @ backend/contest_hub.db                   │
│  ├─ Lightweight file-based DB                      │
│  ├─ Auto-created on startup                        │
│  └─ Sample data initialized                        │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

## Deployment Architecture (Production)

```
┌────────────────────────────────────────────────────────┐
│              Production Deployment                      │
│                                                        │
│  Frontend:                                             │
│  npm run build → dist/                                 │
│  └─ Deploy to: Vercel / Netlify / AWS S3 + CloudFront │
│                                                        │
│  Backend:                                              │
│  pip install gunicorn                                  │
│  gunicorn main:app --workers 4 \                       │
│    --worker-class uvicorn.workers.UvicornWorker        │
│  └─ Deploy to: Render / Railway / Heroku / AWS        │
│                                                        │
│  Database:                                             │
│  PostgreSQL (or upgrade from SQLite)                   │
│  └─ Hosted: AWS RDS / Heroku Postgres / etc           │
│                                                        │
│  DNS & SSL:                                            │
│  └─ CloudFlare / AWS Route53 + ACM                     │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## File Organization

```
contest-stats-3/
│
├── 📄 README.md                    (Main documentation)
├── 📄 SETUP.md                     (Setup guide)
├── 📄 GETTING_STARTED.md           (Quick start)
├── 📄 QUICK_REFERENCE.md           (Commands)
├── 📄 IMPLEMENTATION_SUMMARY.md    (Summary)
├── 📄 ARCHITECTURE.md              (This file)
├── 📄 .gitignore                   (Git rules)
├── 🔧 setup.bat                    (Windows setup)
├── 🔧 setup.sh                     (Linux/Mac setup)
├── 🧪 test_services.py             (Service tester)
│
├── 📁 frontend/
│   ├── 📁 src/
│   │   ├── 📁 components/
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   ├── ContestCard.tsx
│   │   │   ├── AdminPanel.tsx
│   │   │   └── Button.tsx
│   │   ├── 📁 pages/
│   │   │   ├── HomePage.tsx
│   │   │   └── AdminPage.tsx
│   │   ├── 📁 lib/
│   │   │   └── api.ts
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.css
│   ├── 📄 package.json
│   ├── 📄 vite.config.ts
│   ├── 📄 tailwind.config.js
│   ├── 📄 tsconfig.json
│   ├── 📄 index.html
│   └── 📄 README.md
│
└── 📁 backend/
    ├── main.py
    ├── database.py
    ├── models.py
    ├── init_db.py
    ├── 📁 routers/
    │   ├── __init__.py
    │   └── contests.py
    ├── requirements.txt
    ├── contest_hub.db (created on first run)
    └── 📄 README.md
```

---

**Architecture Complete** ✅

This document provides a comprehensive visual and structural overview of the PREHSG Contest Hub application.
